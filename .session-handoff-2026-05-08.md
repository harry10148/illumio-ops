# Session Handoff — 2026-05-08 (final state)

**Session 結束狀態。新 session 啟動時優先讀此檔。**

## 一句話總結

對整個 illumio-ops 專案做了 4 路平行 codebase + docs review（從零基礎新手視角）→ 整合成優先排序報告 → 依使用者指示 A/B/C/D 順序執行（A=onboarding 文件、B=LICENSE/CONTRIBUTING 跳過、C=CI infra、D=既有 plan 對齊）。CI 基礎建設首次落地（`.github/workflows/ci.yml` + `pytest.ini`）。**4 個 commits 已 ship 到 local main，origin 尚未 push**。

## 當前 git 狀態

- **分支**: `main`，本地領先 `origin/main` 4 commits（未 push）
- **HEAD**: 本 session 結尾的 handoff archive commit
- **本 session commits（時序）**:
  1. `33deb7d` docs(readme): clarify newcomer onboarding path; fix Glossary_zh docmap
  2. `dfbbaf6` ci: bootstrap GitHub Actions workflow + central pytest config
  3. `592a59a` docs(plan): mark Batch 4 (H4/H5/H6) DONE in code-review-fixes status
  4. (handoff archive — 本檔案的 commit)
- **工作目錄**: 乾淨（除本 handoff 自身）
- **無 open PR / 無 stale branch / 無 worktree**

## 本 session 完成的具體改動

### A — Onboarding 文件修復 ✅
- `README.md` Quick Start 重寫：新增 **Prerequisites**（PCE API Key 取得位置、網路、Python venv、self-signed verify_ssl 提示）、**Run**、**Verify it worked**（首次啟動依序應見 3 個訊號）
- `README.md` 修正過時的 First Login 段落 — 預設密碼是 well-known `illumio`（不是「auto-generated」），引用 `src/config.py:200`
- `README_zh.md` 同步
- `docs/Glossary_zh.md` docmap 補 Glossary + Troubleshooting 兩列（EN 11 列 vs ZH 9 列 → 對齊）

### C — CI infrastructure ✅
- `pytest.ini`（新增）：`testpaths`/`python_files`/`addopts -ra --tb=short` + `markers` (slow/integration/requires_pce)
- `.github/workflows/ci.yml`（新增）：
  - 觸發：push to main / PR to main
  - Matrix：Python 3.10、3.11
  - **硬閘**：`pip install` / `check_doc_links.py` / `pytest`（這 3 項今天本機都通過）
  - **軟閘 `continue-on-error: true`**（每個都附註解何時可升硬閘）：
    - `audit_i18n_usage.py` — 4 個 pre-existing zh_TW placeholder leak（B 類 3、D 類 1）
    - `mypy src/api_client.py src/analyzer.py src/reporter.py` — 116 個傳遞性錯誤；待 Batch 5 Task 5.3 (M11) 完成
    - `ruff check src tests scripts` — 尚無 ruff config
- 本機驗證：970 tests collect、YAML 解析通過、doc-links 通過

### D — 既有 plan 對齊 ✅（reading-only）
- 讀 `docs/superpowers/plans/2026-05-01-code-review-fixes.md` 確認（**plan 文件原本 Implementation Status 過時，本 session 已回填**）：
  - Batch 1（H1/H2/H3/M4/M8/L1/L4 — 安全急迫）→ DONE 7/7 (`09357b1`)
  - Batch 2（M5/M6/M7/L2/L3/L6 — 行為一致性）→ DONE 6/7 (M6 deferral)
  - Batch 3（M1/M2/M3/L5 — XSS/CSP/範本 i18n）→ DONE 4/4
  - **Batch 4（H4/H5/H6 巨型重構）→ DONE 3/3** — 2026-05-02 連授 3 個子 plan + 連 ship 3 個 release：`v3.22.0-h4-i18n` / `v3.23.0-h5-gui-blueprints` / `v3.24.0-h6-cli-menus (Batch 4 complete)`（plan 文件原本誤標 DEFERRED，因為 Implementation Status 段落最後更新於 2026-05-02 早上、而 Batch 4 是當天下午才 ship 完）
  - Batch 5（M9/M10/M11/L7/L8/L9/L10 — 測試型別衛生）→ **真的 NOT STARTED**；被 2026-05-06 的 Track A/B/C/D sprint 插隊，從 2026-05-02 排到現在沒人接
- 本次 review 新發現的「過大檔」部分與 Batch 4 已處理過的範疇重疊（GUI Blueprint 已拆，但 `_helpers.py` 871 行可能是新留下的集中點），其餘屬於 Batch 5 範疇

## 跳過的項目（保留可追溯）

### B — LICENSE + CONTRIBUTING ⏭️ 使用者主動跳過
- AskUserQuestion：使用者選「先跳過 LICENSE」「跳過 CONTRIBUTING」
- 後續決定後再做；MIT / Apache 2.0 / Proprietary 都未承諾

### docs/superpowers/ 移到非使用者目錄 ⏭️ 使用者明示禁動
- 使用者：「superpowers產物不要異動，他是跨seesion用的」
- 雖在 review 中標為「應移出 docs/」，但使用者規則覆寫此建議

### .session-handoff-2026-05-07.md 移除 ⏭️ 慣例保留
- 上次 commit `a54af4a` 的訊息明示「Kept under the dot-prefix convention so it does not appear in routine listings」
- 所以這是刻意的歸檔，不是該移除的雜物

## Review 中發現但 **未處理** 的項目（供後續挑著做）

### 一級（newcomer 仍會碰到）
- **執行模式 `--monitor-gui` 與 click subcommand 不一致** — README 提此 flag，但 `_CLICK_SUBCOMMANDS` 不含 monitor-gui，僅 legacy argparse 支援。歸屬 Batch 5 L10
- **5 個測試在乾淨環境收集失敗**（缺 `freezegun` / `responses`）— 已在 `requirements-dev.txt` 列出，但 README 沒明示安裝步驟。CI 已涵蓋此安裝；本機開發者仍可能踩雷

### 二級（清晰度與工程品質）
- **EN `Report_Modules.md` 沒提示 ZH 版有 Appendix A**（148 行 persona-based 評分）
- **`cli-command-map.md` / `fonts-vendoring.md` 只有英文版**（破壞 EN/ZH 對稱）
- **沒有 Upgrade / Migration Guide**
- **沒有 LICENSE**（使用者刻意保留決策彈性）

### 三級（部署硬化）
- **systemd 缺 `PrivateTmp=true`、`TimeoutStartSec`** — `deploy/illumio-ops.service`
- **NSSM 沒設 auto-recovery** — Windows 服務崩潰後不會重啟
- **`install.sh` 用 rsync 跨 fs 檢查、`install.ps1` 用 Robocopy 沒對應檢查**（平台 parity 落差）
- **`preflight.sh` 寫死 `/opt`** — 自訂 `--install-root` 會繞過檢查
- **Argon2 參數偏保守**（`time_cost=3, memory=65536`），建議 `time=8, memory=262144`
- **CSRF token timeout 28800s 寫死**，沒 config 化

## Test 健康度

- **970 tests collected**（pytest --collect-only）
- 本機 `pytest` 未實際 run（plan 註：venv 是 `/home/harry/dev/illumio-ops` 路徑下建的、shebang 有問題；改用 `venv/bin/python3 -m pytest`）
- doc-links：`OK — all local links resolve`
- i18n audit：4 個 pre-existing 殘留（不是本 session 引起；與 Batch 5 對齊）

## Mem0 寫入（本 session）

- Memory 1：illumio-ops 慣例 — `docs/superpowers/` 與 `.session-handoff-*.md` 是刻意保留的跨 session 歸檔，不要移除/重定位
- Memory 2：2026-05-08 repo state snapshot（Batch 1-3 done、Batch 4-5 等子 plan、CI 軟閘待 Batch 5）

## 下次 session 可能的方向

1. **commit + push 本 session 改動**（推薦）— 5 個未追蹤/修改檔案；分 2 commits 比較乾淨：
   - commit 1（onboarding）：`README.md` + `README_zh.md` + `docs/Glossary_zh.md`
   - commit 2（CI infra）：`.github/workflows/ci.yml` + `pytest.ini`
2. **正式啟動 Batch 5 子 plan**（mypy / ruff 軟閘升硬閘的前提）
3. **挑做 review 三級硬化**：systemd `PrivateTmp` + NSSM recovery + Argon2 參數 — 短小且獨立
4. **決定 LICENSE**（先有 LICENSE 才好開源 / 對外）
5. **不要動** `docs/superpowers/`、`.session-handoff-*.md`、Track E

## 環境踩過的坑（沿襲 2026-05-07，未變動）

- **PYTHONPATH 雙路徑**: `PYTHONPATH=$(pwd):$(pwd)/venv/lib/python3.12/site-packages python3 -m pytest`
- **venv shebang 路徑壞**: 用 `venv/bin/python3 -m pytest` 不要用 `venv/bin/pytest`
- **`/usr/bin/git` 直走**: 跳過 rtk wrapper 避免吃輸出
- **rg 不在 PATH**: 用 `grep -rEn` fallback
- **illumio-ops.py dispatcher**: 新增 click subcommand 必須同步加入 `_CLICK_SUBCOMMANDS` set

## 本 session 不適用 / 未驗證的工具

- **Telegram MCP**: 未測試本 session 是否仍 disconnect
- **Playwright MCP**: 未使用
- **本機 `pytest` 完整跑一遍**: 只做 `--collect-only`，未執行測試本體（CI 會跑）
